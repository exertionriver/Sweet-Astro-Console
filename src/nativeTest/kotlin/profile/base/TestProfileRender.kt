package profile.base

import astro.base.Chart
import astro.render.RenderAspect
import console.*
import kotlin.test.Test

class TestProfileRender {
/*
    @ExperimentalUnsignedTypes
    @Test
    fun printProfileDetails() {

        val firstProfile = Profiles.getDefaultProfile(Profiles.PROFILE_1)
        val secondProfile = Profiles.getDefaultProfile(Profiles.PROFILE_2)

        val firstProfileChart = Chart.getNatalChartForSnapshot(firstProfile.celestialSnapshot, AnalysisStates.NO_ANALYSIS)// AnalysisStates.ROMANTIC_ANALYSIS)
        val secondProfileChart = Chart.getNatalChartForSnapshot(secondProfile.celestialSnapshot, AnalysisStates.NO_ANALYSIS)//, AnalysisStates.ROMANTIC_ANALYSIS)

        println(RenderAspect.getLabel(firstProfileChart.getChartValue(), AspectValue.POSITIVE_VALUE) + ", " + RenderAspect.getLabel(firstProfileChart.getChartValue(), AspectValue.NEGATIVE_VALUE))
        println(RenderAspect.getLabel(secondProfileChart.getChartValue(), AspectValue.POSITIVE_VALUE) + ", " + RenderAspect.getLabel(secondProfileChart.getChartValue(), AspectValue.NEGATIVE_VALUE))

        val pollChart = Chart.getDefaultChartForSnapshots(firstProfile.celestialSnapshot, AnalysisStates.NO_ANALYSIS, secondProfile.celestialSnapshot, ChartStates.SYNASTRY_CHART)

        println(RenderAspect.getLabel(pollChart.getChartValue(), AspectValue.POSITIVE_VALUE) + ", " + RenderAspect.getLabel(pollChart.getChartValue(), AspectValue.NEGATIVE_VALUE))

        for (aspect in pollChart.aspects) {
            println (RenderAspect.getLabel(aspect))
        }

    }
*/
}